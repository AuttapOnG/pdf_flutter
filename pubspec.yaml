name: pdf_flutter
description: Displaying PDF from Network, File and assets easily like we display
  Image in Flutter Widget.
version: 2.0.0
homepage: https://github.com/erluxman/pdf_flutter

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  custom_path_provider:
#    path: ./custom_path_provider
    git:
      url: https://github.com/AuttapOnG/custom_path_provider.git
      ref: fix/mutiple-class
  custom_flutter_cache_manager:
#    path: ./custom_flutter_cache_manager
    git:
      url: https://github.com/AuttapOnG/custom_flutter_cache_manager.git
      ref: fix/mutiple-class

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  plugin:
    platforms:
      android:
        package: com.erluxman.pdf_flutter
        pluginClass: PdfFlutterPlugin
      ios:
        pluginClass: PdfFlutterPlugin
